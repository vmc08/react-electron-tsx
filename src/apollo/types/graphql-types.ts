export interface IAccount {
  accountId: number,
  username: string,
  firstname: string,
  lastname: string,
  created: string,
  level: number,
  verified: boolean,
  onboarded: boolean,
  scored: boolean,
}
